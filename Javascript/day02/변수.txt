변수 & 상수

변수
ex> 나이는 매년 바뀐다.
상수
ex> 생년월일은 변할 수 없다.

var 나이 : 나이 라는 이름의 변수를 만듦

var 나이 = 30
나이 = 나이(30) + 1 -> 나이 = 31
-a-    -----b-----     -a-  -b-

var   : 변수 (지금은 사용하지 않는다.)
----
let   : 변수
const : 상수

const 출생년도 = 1000; // 무조건 한 줄에 작성 추후 변경 불가능
let 나이; // 추후 변경 얼마든지 가능함

나이 = 현재년도 - 출생년도 + 1;
